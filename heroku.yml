setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./Run migrate
  image: web
run:
  web: ./Run serve --env production --hostname 0.0.0.0 --port $PORT
